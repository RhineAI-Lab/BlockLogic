import { App } from '../../../../src/app/app';

App.LIST = [60,70,80,90,100];

for (let count = 0; count < 12345; count++) {
    console.log('HelloWorld');
}

"ABC123".replace(/[A-Z]/g, 0xFFFFFF);

const cls = new App(true);

const json = {
    "name": "John",
    "age": 30,
    "city": "Shanghai",
};
JSON.stringify(json);

// This is a theme test file