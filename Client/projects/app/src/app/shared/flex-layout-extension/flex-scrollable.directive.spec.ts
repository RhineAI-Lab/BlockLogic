import { FlexScrollableDirective } from './flex-scrollable.directive';

describe('FlexExtensionDirective', () => {
  it('should create an instance', () => {
    const directive = new FlexScrollableDirective();
    expect(directive).toBeTruthy();
  });
});
